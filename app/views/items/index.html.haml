%div.page

  %h1#title Welcome to Your Online Pet Store!
  = link_to 'New Item', new_item_path
  %h2 Here are the available pets:


  - @items.each do |item|
    %ul
      %li= link_to "#{item.title}", item_path(item.id)
  %p
  = link_to 'Home', root_path
